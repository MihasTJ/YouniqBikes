timezone: Europe/Warsaw
source: /

collections_config:
  pages:
    name: Strony
    path: ''  # Puste oznacza główny folder, tam gdzie masz index.html
    parser: html
    output: true
    _enabled_editors:
      - visual
      - source

# To aktywuje Twoje klasy .editable w edytorze wizualnym
_editables:
  content:
    active: true
    allow_html: true